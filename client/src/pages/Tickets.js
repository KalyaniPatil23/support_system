import React from 'react';
import TicketList from '../components/TicketList';

const Tickets = () => {
    return (
        <div>
            <TicketList />
        </div>
    );
};

export default Tickets;
